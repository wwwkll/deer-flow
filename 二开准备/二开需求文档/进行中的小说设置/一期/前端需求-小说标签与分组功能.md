# 一期需求文档：进行中的小说设置 - 标签系统与分组功能

> 目标：为对话系统添加小说标签功能，支持在新建对话时选择小说标签，并在对话列表中按标签分组展示

---

## 1. 需求概述

### 1.1 功能目标
- 在新建对话时，允许用户从枚举列表中选择当前正在进行的小说标签（非必填）
- 标签枚举值来源于工作目录下 `workspace/book` 文件夹中的所有文件夹名称
- 标签选择后存储为**对话级别的全局变量**（系统变量），后续可读取
- 在左侧对话列表页面增加按标签分组的功能

### 1.2 业务规则

1. **标签只能在创建新对话时选择**，创建后不可修改
2. **标签为非必填项**：用户可以不选标签，此时不创建变量
3. **标签值来源**：扫描 `{base_dir}/threads/{thread_id}/user-data/workspace/book/` 下的所有一级文件夹名称
4. **标签存储**：存储为对话级别的全局变量（系统变量），`is_system=True`，`llm_editable=False`
5. **分组展示**：
   - 左侧对话列表按标签分组显示
   - 未设置标签的对话归为"未分类"分组
   - **每次打开页面时，所有分组默认折叠状态，不保留用户的展开/折叠偏好**
6. **目录来源说明**：`book` 目录下的文件夹由 Agent 在运行时自动创建，前端只需要读取现有的目录作为标签枚举值

---

## 2. 技术栈

| 技术 | 用途 |
|------|------|
| Next.js + React 18 | 前端框架 |
| TypeScript 5.x | 类型系统 |
| LangGraph SDK | 线程状态管理 |
| Shadcn UI | UI 组件库 |
| Tailwind CSS | 样式 |
| FastAPI + Python | 后端框架 |
| **全局变量系统** | **标签存储（已开发完成）** |

---

## 3. 数据模型设计

### 3.1 存储方案：对话级全局变量（系统变量）

**利用现有的全局变量系统**（已开发完成，使用 SQLite 存储）：

**存储的是小说目录的完整路径**（如 `/mnt/shared-data/novels/book/小说A`），但 UI 显示时只提取最后一段（小说名）：

```typescript
// 创建对话后，调用 API 设置对话级全局变量
await fetch(`${getBackendBaseURL()}/api/global-variables/threads/${threadId}/novel_toc`, {
  method: "PUT",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({
    value: "/mnt/shared-data/novels/book/小说A",  // 存储完整路径
    description: "当前正在进行的小说目录",
    is_system: true,        // 系统变量
    llm_editable: false,    // 不允许大模型修改
  }),
});
```

**存储位置**：SQLite 数据库 `{DEER_FLOW_HOME}/global_variables.db`

**变量结构**：
```json
{
  "variables": {
    "workdir": { ... },
    "novel_toc": {
      "value": "/mnt/shared-data/novels/book/小说A",
      "description": "当前正在进行的小说目录",
      "is_system": true,
      "llm_editable": false,
      "updated_at": "2026-04-27T10:00:00Z",
      "updated_by": "api"
    }
  }
}
```

**显示规则**：
- 存储：`novel_toc` = 完整路径（如 `/mnt/shared-data/novels/book/小说A`）
- 显示：提取路径最后一段（如"小说A"）

### 3.2 标签枚举 API

新增后端 API 接口，用于获取可用的小说标签列表：

```
GET /api/novel-tags/

Response:
{
  "tags": ["/mnt/shared-data/novels/book/小说A", "/mnt/shared-data/novels/book/小说B", ...]
}
```

**注意**：返回的是完整路径，但 UI 显示时会提取最后一段作为小说名。

---

## 4. 前端技术方案

### 4.1 新增组件

#### 4.1.1 NovelTagSelector（小说标签选择器）

位置：`frontend/src/components/workspace/novel-tag-selector.tsx`

功能：
- 下拉选择器组件，用于在新建对话时选择小说标签
- 通过 API 获取标签枚举值
- 支持搜索和筛选
- **支持清空选择（设置为 undefined）**

```typescript
interface NovelTagSelectorProps {
  value?: string;           // undefined 表示未选择
  onChange: (tag: string | undefined) => void;
  disabled?: boolean;
}
```

UI 设计：
```
┌──────────────────────────────────────────┐
│ 选择进行中的小说（可选）：[下拉选择框 ▼] │
│  选项：清空 / 小说A / 小说B / 小说C      │
└──────────────────────────────────────────┘
```

#### 4.1.2 GroupedChatList（分组对话列表）

位置：扩展 `frontend/src/components/workspace/recent-chat-list.tsx`

功能：
- 按对话级全局变量中的 `novel_toc` 对对话进行分组
- 从路径中提取最后一段作为显示名称
- 每个分组可展开/折叠
- **每次刷新页面时，所有分组默认折叠**
- 未设置标签的对话归为"未分类"分组

UI 设计：
```
┌─────────────────────────────────┐
│ ▶ 📁 小说A (3)                  │  ← 默认折叠
│ ▶ 📁 小说B (2)                  │  ← 默认折叠
│ ▶ 📁 未分类 (5)                 │  ← 默认折叠
└─────────────────────────────────┘

用户点击后展开：
┌─────────────────────────────────┐
│ ▼ 📁 小说A (3)                  │
│   ├─ 对话1                      │
│   ├─ 对话2                      │
│   └─ 对话3                      │
│ ▶ 📁 小说B (2)                  │
│ ▶ 📁 未分类 (5)                 │
└─────────────────────────────────┘
```

### 4.2 修改现有组件

#### 4.2.1 InputBox（输入框）

文件：`frontend/src/components/workspace/input-box.tsx`

修改点：
- 在新建对话模式（`isNewThread=true`）时，显示小说标签选择器
- 将选中的标签值暂存在组件状态中

```typescript
// 在 InputBox 组件中新增
{isNewThread && (
  <NovelTagSelector
    value={novelTag}
    onChange={(tag) => setNovelTag(tag)}
  />
)}
```

#### 4.2.2 RecentChatList（最近对话列表）

文件：`frontend/src/components/workspace/recent-chat-list.tsx`

修改点：
- 加载对话列表时，同时获取每个对话的全局变量
- 按 `novel_toc` 变量值分组（实际按提取后的显示名分组）
- 增加分组展开/折叠状态管理（使用 `useState`，不持久化）
- 修改渲染逻辑，支持分组展示
- **从 `novel_toc` 完整路径中提取最后一段作为分组显示名称**

```typescript
// 分组逻辑
const groupedThreads = useMemo(() => {
  const groups: Record<string, AgentThread[]> = {};
  threads.forEach(thread => {
    // 从对话级全局变量中读取 novel_toc
    // 提取路径最后一段作为显示名
    const tocValue = thread.globalVariables?.novel_toc;
    const tag = tocValue ? tocValue.split("/").filter(Boolean).pop() : "__undefined__";
    if (!groups[tag]) {
      groups[tag] = [];
    }
    groups[tag].push(thread);
  });
  return groups;
}, [threads]);

// 分组展开/折叠状态（使用 Map 管理多个分组的状态）
const [expandedGroups, setExpandedGroups] = useState<Set<string>>(new Set());

// 切换分组展开/折叠
const toggleGroup = useCallback((groupName: string) => {
  setExpandedGroups(prev => {
    const next = new Set(prev);
    if (next.has(groupName)) {
      next.delete(groupName);
    } else {
      next.add(groupName);
    }
    return next;
  });
}, []);
```

### 4.3 API 调用

#### 4.3.1 获取标签列表

```typescript
// frontend/src/core/novel-tags/hooks.ts

import { useQuery } from "@tanstack/react-query";
import { getBackendBaseURL } from "@/core/config";

export function useNovelTags() {
  return useQuery<{ tags: string[] }>({
    queryKey: ["novel-tags"],
    queryFn: async () => {
      const response = await fetch(`${getBackendBaseURL()}/api/novel-tags`);
      if (!response.ok) {
        throw new Error("Failed to fetch novel tags");
      }
      return response.json();
    },
    staleTime: 5 * 60 * 1000,  // 5 分钟内不重新请求
  });
}
```

#### 4.3.2 创建对话后保存标签

标签存储为对话级全局变量：

```typescript
// frontend/src/core/threads/hooks.ts - useThreadStream

const thread = useStream<AgentThreadState>({
  // ... 其他配置
  onCreated(meta) {
    handleStreamStart(meta.thread_id);
    setOnStreamThreadId(meta.thread_id);

    // 如果选择了小说标签，保存为对话级全局变量（系统变量）
    // 注意：存储的是完整路径，value 就是 context.novel_toc
    if (context.novel_toc) {
      void fetch(
        `${getBackendBaseURL()}/api/global-variables/threads/${meta.thread_id}/novel_toc`,
        {
          method: "PUT",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({
            value: context.novel_toc,  // 完整路径
            description: "当前正在进行的小说目录",
            is_system: true,
            llm_editable: false,
          }),
        }
      ).catch(() => ({}));
    }
  },
  // ...
});
```

#### 4.3.3 加载对话列表时读取标签

需要获取每个对话的全局变量：

```typescript
// 在加载对话列表时，并行获取每个对话的变量
const loadThreadsWithNovelTags = async (threads: AgentThread[]) => {
  const threadsWithTags = await Promise.all(
    threads.map(async (thread) => {
      const response = await fetch(
        `${getBackendBaseURL()}/api/global-variables/threads/${thread.thread_id}`
      );
      if (response.ok) {
        const data = await response.json();
        const novelTocVar = data.variables?.find((v: any) => v.key === "novel_toc");
        // 提取路径最后一段作为显示名称
        const tocValue = novelTocVar?.value;
        const displayName = tocValue ? tocValue.split("/").filter(Boolean).pop() : undefined;
        return {
          ...thread,
          globalVariables: {
            novel_toc: tocValue,
            novel_toc_display: displayName,
          },
        };
      }
      return { ...thread, globalVariables: { novel_toc: undefined, novel_toc_display: undefined } };
    })
  );
  return threadsWithTags;
};
```

---

## 5. 后端技术方案

### 5.1 新增 API 端点

#### 5.1.1 获取小说标签列表

文件：`backend/app/api/routes/novel_tags.py`

```python
from fastapi import APIRouter, HTTPException
from deerflow.config.paths import get_paths
import logging
import time

logger = logging.getLogger(__name__)
router = APIRouter()

# 简单缓存
_novel_tags_cache = {
    "tags": [],
    "last_updated": 0,
    "ttl": 300  # 5 分钟缓存
}

@router.get("/api/novel-tags")
async def get_novel_tags():
    """
    获取所有可用的小说标签
    
    扫描所有线程的工作目录下的 book 文件夹，
    返回所有小说文件夹名称作为标签枚举值
    """
    current_time = time.time()
    
    # 检查缓存是否有效
    if (
        _novel_tags_cache["tags"]
        and current_time - _novel_tags_cache["last_updated"] < _novel_tags_cache["ttl"]
    ):
        return {"tags": _novel_tags_cache["tags"]}
    
    try:
        paths = get_paths()
        base_dir = paths.base_dir
        threads_dir = base_dir / "threads"
        
        if not threads_dir.exists():
            return {"tags": []}
        
        tags = set()
        
        # 遍历所有线程目录
        for thread_dir in threads_dir.iterdir():
            if not thread_dir.is_dir():
                continue
            
            # 检查 book 目录
            book_dir = (
                thread_dir / "user-data" / "workspace" / "book"
            )
            
            if book_dir.exists() and book_dir.is_dir():
                # 收集所有一级文件夹名称
                for item in book_dir.iterdir():
                    if item.is_dir():
                        tags.add(item.name)
        
        sorted_tags = sorted(list(tags))
        
        # 更新缓存
        _novel_tags_cache["tags"] = sorted_tags
        _novel_tags_cache["last_updated"] = current_time
        
        return {"tags": sorted_tags}
        
    except Exception as e:
        logger.error(f"Failed to get novel tags: {e}")
        raise HTTPException(
            status_code=500,
            detail=f"Failed to get novel tags: {str(e)}"
        )
```

### 5.2 注册路由

文件：`backend/app/api/routes/__init__.py`

```python
from . import novel_tags

# 在路由注册处添加
app.include_router(novel_tags.router)
```

---

## 6. 业务流程

### 6.1 创建对话流程

```
用户点击"新建对话"
    ↓
前端获取小说标签列表（GET /api/novel-tags/）
    ↓
显示标签选择器（显示小说名，存储完整路径）
    ↓
用户选择标签（或不选）
    ↓
用户输入消息并提交
    ↓
前端创建线程
    ↓
在 onCreated 回调中：
  - 如果选择了标签，调用 PUT /api/global-variables/threads/{thread_id}/novel_toc
  - value 为完整路径（如 /mnt/shared-data/novels/book/小说A）
  - 设置为系统变量（is_system=True, llm_editable=False）
    ↓
对话创建完成，左侧列表刷新显示
```

### 6.2 对话列表分组流程

```
前端加载对话列表
    ↓
对每个对话，获取其全局变量（GET /api/global-variables/threads/{thread_id}）
    ↓
提取 novel_toc 变量值
    ↓
按 novel_toc 分组（undefined 归为"未分类"）
↓ ↓
渲染分组列表，所有分组默认折叠
显示时提取路径最后一段作为分组名称
    ↓
用户可点击分组标题展开/折叠
    ↓
刷新页面时，重新折叠所有分组
```

---

## 7. 文件变更清单

### 7.1 前端新增文件

| 文件路径 | 说明 |
|---------|------|
| `frontend/src/components/workspace/novel-tag-selector.tsx` | 小说标签选择器组件 |
| `frontend/src/core/novel-tags/hooks.ts` | 标签 API hooks |
| `frontend/src/core/novel-tags/types.ts` | 标签类型定义 |

### 7.2 前端修改文件

| 文件路径 | 修改内容 |
|---------|---------|
| `frontend/src/components/workspace/input-box.tsx` | 添加标签选择器（仅新建对话时显示），使用 `novel_toc` |
| `frontend/src/components/workspace/recent-chat-list.tsx` | 添加分组功能，从 `novel_toc` 路径提取显示名 |
| `frontend/src/core/threads/hooks.ts` | 在 onCreated 回调中保存 `novel_toc` 到全局变量 |
| `frontend/src/core/threads/types.ts` | 类型定义中添加 `novel_toc?: string` |

### 7.3 后端新增文件

| 文件路径 | 说明 |
|---------|------|
| `backend/app/gateway/routers/novel_tags.py` | 标签 API 路由（带简单缓存） |

### 7.4 后端修改文件

| 文件路径 | 修改内容 |
|---------|---------|
| `backend/app/gateway/app.py` | 注册 novel_tags 路由 |
| `backend/app/gateway/routers/__init__.py` | 导出 novel_tags 模块 |

---

## 8. 测试要点

### 8.1 前端测试
- [ ] 标签选择器能正确显示枚举值
- [ ] 标签选择器支持清空（设置为 undefined）
- [ ] 新建对话时选择标签后，标签能正确存储为对话级全局变量
- [ ] 新建对话时不选标签，不创建 novel_tag 变量
- [ ] 对话列表能按标签正确分组
- [ ] 未设置标签的对话归为"未分类"
- [ ] 分组展开/折叠功能正常
- [ ] 刷新页面后，所有分组恢复折叠状态

### 8.2 后端测试
- [ ] GET /api/novel-tags 能正确扫描并返回标签列表
- [ ] 缓存机制正常工作（5 分钟内返回缓存数据）
- [ ] PUT /api/global-variables/threads/{thread_id}/novel_tag 能正确创建系统变量
- [ ] 查询对话变量时能正确返回 novel_tag

---

## 9. 注意事项

1. **缓存策略**：标签列表使用 5 分钟简单缓存，缓存过期后自动刷新
2. **向后兼容**：已有对话没有 novel_toc 变量，前端需做兼容处理（归为"未分类"）
3. **国际化**：标签选择器和分组标题需要支持 i18n
4. **用户体验**：标签选择器应明确标注"可选"，避免用户困惑
5. **分组排序**：建议按字母顺序或创建时间排序分组，"未分类"固定放在最后
6. **系统变量特性**：novel_toc 设置为 `is_system=True`，`llm_editable=False`，前端只读展示，大模型不可修改
7. **路径存储**：`novel_toc` 存储的是完整路径（如 `/mnt/shared-data/novels/book/小说A`），UI 显示时提取最后一段（如"小说A"）
8. **标签来源**：标签来源于配置的 sandbox.mounts 下的 book/books 目录，最多扫描 3 级深度

---

## 10. 开发排期建议

| 阶段 | 工作内容 | 预估工作量 |
|------|---------|-----------|
| 1 | 后端 API 开发（标签列表接口 + 缓存） | 0.5 天 |
| 2 | 前端类型定义和 hooks 开发 | 0.5 天 |
| 3 | NovelTagSelector 组件开发 | 1 天 |
| 4 | InputBox 集成标签选择器 | 0.5 天 |
| 5 | RecentChatList 分组功能开发 | 1 天 |
| 6 | 联调测试 | 1 天 |
| 7 | 代码审查和优化 | 0.5 天 |

**总计：约 5 天**
