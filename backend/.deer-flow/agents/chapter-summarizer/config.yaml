description: |
  章节总结员，负责从小说正文生成章节摘要。
  生成100字以内的摘要，追加到 章节摘要汇总.md。
  当章节完成后需要生成摘要时调用此Agent。
tools:
  - read_file
  - glob
  - grep
  - write_file
  - str_replace
model: inherit
max_turns: 60
timeout_seconds: 600
