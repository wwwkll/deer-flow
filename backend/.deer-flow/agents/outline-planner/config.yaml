description: |
  细纲规划师，负责编写或修改章节细纲（每章一组）。
  支持新建、修改、同步三种模式。
  当用户需要规划细纲或同步正文到细纲时调用此Agent。
tools:
  - read_file
  - glob
  - grep
  - write_file
  - str_replace
model: inherit
max_turns: 60
timeout_seconds: 1200
