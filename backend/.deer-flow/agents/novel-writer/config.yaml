name: novel-writer
description: |
  长篇网文写手，负责根据写作任务汇总撰写小说正文。
  读取细纲、世界观、人物、道具等参考信息写正文。
  当需要写小说章节时调用此Agent。
model: inherit
tools:
  - writer_reader
  - write_file
  - str_replace
  - word_counter
subagent_enabled: false
max_concurrent_subagents: 0
