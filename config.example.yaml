# Configuration for the DeerFlow application
config_version: 8

# ============================================================================
# Logging Configuration
# ============================================================================
log_level: info

token_usage:
  enabled: false

# ============================================================================
# Models Configuration
# ============================================================================
# Define one or more LLM models for the agent to use
# Agent configs can override by name (e.g. model: o4-mini)

models:
  # Primary model configuration
  - name: qwen-3-6-online
    display_name: Qwen 3.6 Online
    use: langchain_openai:ChatOpenAI
    model: qwen-3-6-online
    # Authentication: API key or username:password format
    api_key: $OPENAI_API_KEY
    # Base URL for the model endpoint
    base_url: https://your-model-api-endpoint/v1
    # Request timeout in seconds
    request_timeout: 60000.0
    # Maximum number of retries for failed requests
    max_retries: 4
    # Maximum number of tokens in the model's response
    max_tokens: 80000
    # Temperature for controlling response randomness
    temperature: 0.7
    # Whether the model supports thinking mode
    supports_thinking: false
    # Whether the model supports vision
    supports_vision: false

# ============================================================================
# Tool Groups Configuration
# ============================================================================
# Define groups of tools for organization and access control

tool_groups:
  - name: web
  - name: file:read
  - name: file:write
  - name: bash

# ============================================================================
# Tools Configuration
# ============================================================================
# Configure available tools for the agent to use

tools:
  # Web search tool (uses DuckDuckGo, no API key required)
  - name: web_search
    group: web
    use: deerflow.community.ddg_search.tools:web_search_tool
    max_results: 5

  # Web fetch tool (uses Jina AI reader)
  - name: web_fetch
    group: web
    use: deerflow.community.jina_ai.tools:web_fetch_tool
    timeout: 10

  # Image search tool (uses DuckDuckGo)
  - name: image_search
    group: web
    use: deerflow.community.image_search.tools:image_search_tool
    max_results: 5

  # File operations tools
  - name: ls
    group: file:read
    use: deerflow.sandbox.tools:ls_tool

  - name: read_file
    group: file:read
    use: deerflow.sandbox.tools:read_file_tool

  - name: glob
    group: file:read
    use: deerflow.sandbox.tools:glob_tool
    max_results: 200

  - name: grep
    group: file:read
    use: deerflow.sandbox.tools:grep_tool
    max_results: 100

  - name: write_file
    group: file:write
    use: deerflow.sandbox.tools:write_file_tool

  - name: str_replace
    group: file:write
    use: deerflow.sandbox.tools:str_replace_tool

  # Bash execution tool
  # Active only when using an isolated shell sandbox or when
  # sandbox.allow_host_bash: true explicitly opts into host bash.
  - name: bash
    group: bash
    use: deerflow.sandbox.tools:bash_tool

# ============================================================================
# Tool Search Configuration (Deferred Tool Loading)
# ============================================================================

tool_search:
  enabled: false

# ============================================================================
# Sandbox Configuration
# ============================================================================

uploads:
  auto_convert_documents: false
  pdf_converter: auto

sandbox:
  use: deerflow.sandbox.local:LocalSandboxProvider
  allow_host_bash: false
  mounts:
    - host_path: backend/.deer-flow/shared-data
      container_path: /mnt/shared-data
      read_only: false
  bash_output_max_chars: 20000
  read_file_output_max_chars: 50000
  ls_output_max_chars: 20000

# ============================================================================
# Skills Configuration
# ============================================================================

skills:
  path: skills
  container_path: /mnt/skills

# ============================================================================
# Title Generation Configuration
# ============================================================================

title:
  enabled: true
  max_words: 6
  max_chars: 60
  model_name: null

# ============================================================================
# Summarization Configuration
# ============================================================================

summarization:
  enabled: true
  model_name: null
  trigger:
    - type: tokens
      value: 15564
  keep:
    type: messages
    value: 10
  trim_tokens_to_summarize: 15564
  summary_prompt: null
  preserve_recent_skill_count: 5
  preserve_recent_skill_tokens: 25000
  preserve_recent_skill_tokens_per_skill: 5000
  skill_file_read_tool_names:
    - read_file
    - read
    - view
    - cat

# ============================================================================
# Memory Configuration
# ============================================================================

memory:
  enabled: true
  storage_path: memory.json
  debounce_seconds: 30
  model_name: null
  max_facts: 100
  fact_confidence_threshold: 0.7
  injection_enabled: true
  max_injection_tokens: 2000

# ============================================================================
# Agents API Configuration
# ============================================================================

agents_api:
  enabled: true

# ============================================================================
# Skill Evolution Configuration
# ============================================================================

skill_evolution:
  enabled: false
  moderation_model_name: null

# ============================================================================
# Subagents Configuration
# ============================================================================

subagents:
  timeout_seconds: 900
  max_turns: 50

  # Per-agent overrides
  agents:
    general-purpose:
      timeout_seconds: 1800
      max_turns: 160
    bash:
      timeout_seconds: 300
      max_turns: 80

checkpointer:
  type: sqlite
  connection_string: checkpoints.db

# ============================================================================
# 二开配置：小说写作系统
# ============================================================================
# 使用说明：
#   1. 将下方配置中需要启用的部分取消注释
#   2. 按需修改参数值（如 API Key、模型等）
#   3. 重启服务生效
# ============================================================================

# --- ① Tool Groups ---

# tool_groups:
#   - name: novel              # 小说写作工具组

# --- ② Tools ---

# tools:
#   - name: context_assembler
#     group: novel
#     use: my_tools.context_assembler:context_assembler
#
#   - name: post_write_validator
#     group: novel
#     use: my_tools.post_write_validator:post_write_validator
#
#   - name: ai_trace_detector
#     group: novel
#     use: my_tools.ai_trace_detector:ai_trace_detector
#
#   - name: card_validator
#     group: novel
#     use: my_tools.card_validator:card_validator

# --- ③ Skills ---
# Skills 位于 skills/custom/ 目录，通过 skills.path 自动扫描加载
# 现有技能：
#   - novel-post-write-validator   写作后格式验证
#   - novel-anti-ai-detector       AI 痕迹检测与消除
#   - novel-plan-compliance        规划合规检查
# Agent 在 config.yaml 中通过 skills 字段声明使用：
#   skills:
#     - novel-post-write-validator
#     - novel-anti-ai-detector
#     - novel-plan-compliance

# --- ④ Subagents ---

# subagents:
#   timeout_seconds: 900
#   max_turns: 50
#
#   agents:
#     novel-architect:
#       timeout_seconds: 1800
#       max_turns: 80
#     novel-writer:
#       timeout_seconds: 1800
#       max_turns: 80
#     novel-reviser:
#       timeout_seconds: 1800
#       max_turns: 80
#     continuity-auditor:
#       timeout_seconds: 1200
#       max_turns: 60
#
#   custom_agents:
#     novel-architect:
#       description: |
#         小说架构师，负责新建小说时生成完整基础设定。
#         包括世界观、卷纲、规则、计划、初始状态、伏笔池。
#       system_prompt: |
#         你是小说创作系统的架构师，专门负责新建小说时生成完整的基础架构设定。
#         ## 职责
#         1. 世界观构建  2. 卷纲规划  3. 规则制定  4. 创作计划  5. 初始状态
#         ## 输出文件
#         - story-bible.md、volume-outline.md、book-rules.json
#         - book-plan.md、current-state.md、pending-hooks.md
#       tools:
#         - read_file
#         - write_file
#         - glob
#       model: inherit
#       max_turns: 80
#       timeout_seconds: 1800
#
#     volume-planner:
#       description: 卷纲规划师，负责编写或修改全书卷纲
#       system_prompt: |
#         你是小说创作系统的卷纲规划师，专门负责编写或修改全书的分卷大纲。
#         ## 职责：新建卷纲、修改卷纲、一致性维护、章节分组
#       tools:
#         - read_file
#         - write_file
#         - str_replace
#       model: inherit
#       max_turns: 60
#       timeout_seconds: 1200
#
#     outline-planner:
#       description: 细纲规划师，负责编写或修改章节细纲（每5章一组）
#       system_prompt: |
#         你是小说创作系统的细纲规划师，专门负责编写或修改章节细纲。
#         ## 工作模式：new（新建）、revise（修改）、sync（同步）
#       tools:
#         - read_file
#         - write_file
#         - str_replace
#       model: inherit
#       max_turns: 60
#       timeout_seconds: 1200
#
#     book-rules-manager:
#       description: 本书规则管理器，负责管理 book-rules.json
#       system_prompt: |
#         你是小说创作系统的规则管理器，负责管理 book-rules.json 文件。
#         ## 规则类别：hard_rules、style_guidelines、constraints、banned_words
#       tools:
#         - read_file
#         - str_replace
#       model: inherit
#       max_turns: 40
#       timeout_seconds: 600
#
#     novel-world-organizer:
#       description: 世界观整理员，负责整理当前章节需要的世界观设定
#       system_prompt: |
#         你是小说创作系统的世界观整理员，负责在写作前整理当前章节需要的世界观设定。
#       tools:
#         - read_file
#         - write_file
#       model: inherit
#       max_turns: 40
#       timeout_seconds: 600
#
#     novel-character-organizer:
#       description: 人物整理员，负责整理当前章节出场的人物信息
#       system_prompt: |
#         你是小说创作系统的人物整理员，负责在写作前整理当前章节出场的人物信息。
#       tools:
#         - read_file
#         - write_file
#       model: inherit
#       max_turns: 40
#       timeout_seconds: 600
#
#     novel-item-organizer:
#       description: 道具整理员，负责整理当前章节出场的道具和技能
#       system_prompt: |
#         你是小说创作系统的道具整理员，负责在写作前整理当前章节出场的道具和技能信息。
#       tools:
#         - read_file
#         - write_file
#       model: inherit
#       max_turns: 40
#       timeout_seconds: 600
#
#     novel-writer:
#       description: 长篇网文写手，负责根据写作任务汇总撰写小说正文
#       system_prompt: |
#         你是一个专业的长篇网文写手。
#         ## 写作风格：口语化对白、强情绪强冲突、长短句结合
#         ## 写作要求：冲突升级、对话占比30%、1600字以上
#         ## 输出规则：纯正文、无Markdown格式、无章节标题
#       tools:
#         - read_file
#         - write_file
#       model: inherit
#       max_turns: 80
#       timeout_seconds: 1800
#
#     continuity-auditor:
#       description: 连续性审计师，负责多维度审核小说章节一致性
#       system_prompt: |
#         你是小说创作系统的连续性审计师。
#         ## 审计维度：角色一致性、世界观一致性、叙事连贯性、伏笔管理、写作质量、AI痕迹检测
#       tools:
#         - read_file
#         - write_file
#       model: inherit
#       max_turns: 60
#       timeout_seconds: 1200
#
#     novel-reviser:
#       description: 修稿编辑，负责根据审计报告修改小说正文
#       system_prompt: |
#         你是小说创作系统的修稿编辑。
#         ## 修稿模式：polish、rewrite、rework、anti-detect、spot-fix
#       tools:
#         - read_file
#         - write_file
#         - str_replace
#       model: inherit
#       max_turns: 80
#       timeout_seconds: 1800
#
#     state-settler:
#       description: 结算器，负责将写作输出的事实变更合并到状态文件
#       system_prompt: |
#         你是小说创作系统的结算器。
#         ## 职责：更新 current-state.md、pending-hooks.md、chapter-summaries.md
#       tools:
#         - read_file
#         - write_file
#         - str_replace
#       model: inherit
#       max_turns: 60
#       timeout_seconds: 1200
#
#     chapter-summarizer:
#       description: 章节总结员，负责从小说正文生成章节摘要
#       system_prompt: |
#         你是小说创作系统的章节总结助理。
#       tools:
#         - read_file
#         - write_file
#       model: inherit
#       max_turns: 40
#       timeout_seconds: 600
#
#     card-manager:
#       description: 名片管理器，负责更新小说名片（card.json）
#       system_prompt: |
#         你是小说创作系统的名片管理助理。
#       tools:
#         - read_file
#         - str_replace
#       model: inherit
#       max_turns: 40
#       timeout_seconds: 600
#
#     hook-manager:
#       description: 伏笔管理器，负责管理伏笔池状态
#       system_prompt: |
#         你是小说创作系统的伏笔管理助理。
#         ## 伏笔状态：open、progressing、resolved
#       tools:
#         - read_file
#         - str_replace
#       model: inherit
#       max_turns: 40
#       timeout_seconds: 600
