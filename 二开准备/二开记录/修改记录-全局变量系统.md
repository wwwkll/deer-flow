# 全局变量系统开发记录

**日期**: 2026-04-27
**功能**: 全局变量系统（动态提示词注入）
**开发者**: AI Assistant
**版本**: v1.0（一期）+ v2.0（二期系统变量+模板替换）

---

## 一、需求背景

DeerFlow 项目需要在每次调用大模型时，**动态向系统提示词中注入可变的变量值**。

### 一期需求
1. **增删改查**：大模型可通过工具修改变量；外部业务脚本可通过 API 修改变量
2. **两级隔离**：项目级变量（所有对话共享）+ 对话级变量（单个对话独立）
3. **自动注入**：变量值自动全部注入到系统提示词中

### 二期需求
1. **模板替换**：不再自动注入，改为在 SOUL.md 等提示词模板中用 `{{变量名}}` 语法手动引用
2. **系统变量**：新增默认的系统变量（如 `workdir`），前端不可编辑、大模型不可修改
3. **llm_editable 权限**：每个变量可控制大模型是否能通过工具修改
4. **前端管理页面**：在系统设置中新增"全局变量"管理页面

---

## 二、实现方案

### 2.1 架构设计

```
┌─────────────────────────────────────────────────────────────────┐
│                        前端 (Next.js)                            │
│  设置对话框 → 全局变量管理页面                                    │
│  - 项目级/对话级切换                                              │
│  - 增删改查操作                                                   │
│  - 系统变量只读展示                                               │
└───────────────────────┬─────────────────────────────────────────┘
                        │ REST API
┌───────────────────────▼─────────────────────────────────────────┐
│                    后端 (FastAPI)                                │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ API 路由 (global_variables.py)                            │   │
│  │ - GET /api/global-variables/project                       │   │
│  │ - GET /api/global-variables/threads/{thread_id}           │   │
│  │ - PUT /api/global-variables/project/{key}                 │   │
│  │ - PUT /api/global-variables/threads/{thread_id}/{key}     │   │
│  │ - DELETE /api/global-variables/project/{key}              │   │
│  │ - DELETE /api/global-variables/threads/{thread_id}/{key}  │   │
│  └──────────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ 权限检查                                                    │   │
│  │ - is_system 检查（系统变量不可编辑/删除）                   │   │
│  │ - llm_editable 检查（LLM 不可修改非可编辑变量）             │   │
│  └──────────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ 存储层 (storage.py)                                        │   │
│  │ - 文件存储：{DEER_FLOW_HOME}/global_variables.json        │   │
│  │ - 两级隔离：项目级 + 对话级                                 │   │
│  │ - mtime 缓存策略                                            │   │
│  │ - 系统变量合并：SYSTEM_VARIABLES + 自定义变量              │   │
│  └──────────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ 模板替换 (prompt_injector.py)                              │   │
│  │ - replace_template_variables(): {{变量名}} → 实际值        │   │
│  └──────────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ 中间件 (global_variables_middleware.py)                    │   │
│  │ - 运行时注入：每次大模型调用前替换系统提示词中的模板变量    │   │
│  └──────────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ 工具 (global_variables/tools.py)                           │   │
│  │ - get_variable: 获取变量值                                 │   │
│  │ - set_variable: 设置变量值（检查权限）                      │   │
│  │ - delete_variable: 删除变量（检查权限）                     │   │
│  │ - list_variables: 列出所有变量                             │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 系统变量

```python
# 定义在 storage.py 中
SYSTEM_VARIABLES = {
    "workdir": {
        "value": "/mnt/shared-data",
        "description": "Shared workspace directory",
        "is_system": True,
        "llm_editable": False,
        "updated_at": "system",
        "updated_by": "system",
    },
}
```

**特性**：
- 默认存在，每个用户都有
- 前端展示但不可编辑（显示锁定图标）
- 大模型工具无法修改或删除
- 保存时自动过滤，不写入文件
- 加载时自动合并到变量列表中

### 2.3 模板替换

**语法**：`{{变量名}}`

**示例**（SOUL.md）：
```markdown
当前工作目录：{{workdir}}
你是{{role}}，当前处理第{{chapter}}章。
```

**替换规则**：
1. 对话级变量覆盖项目级变量
2. 未匹配的 `{{变量名}}` 保持原样不替换
3. 替换发生在两处：
   - **加载时**：`get_agent_soul()` 读取 SOUL.md 后替换（项目级变量）
   - **运行时**：中间件在每次大模型调用前替换（含对话级变量）

### 2.4 权限模型

| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `is_system` | bool | false | 系统变量（前端只读、LLM 不可修改） |
| `llm_editable` | bool | true | 大模型是否可通过工具修改 |

**权限矩阵**：

| 操作 | 系统变量 | 自定义+llm_editable=true | 自定义+llm_editable=false |
|------|---------|--------------------------|---------------------------|
| 前端查看 | ✅ | ✅ | ✅ |
| 前端编辑 | ❌ | ✅ | ✅ |
| 前端删除 | ❌ | ✅ | ✅ |
| LLM 读取 | ✅ | ✅ | ✅ |
| LLM 修改 | ❌ | ✅ | ❌ |
| LLM 删除 | ❌ | ✅ | ❌ |

---

## 三、修改清单

### 3.1 后端

| 文件 | 操作 | 说明 |
|------|------|------|
| `backend/packages/harness/deerflow/config/global_variables_config.py` | 新增 | Pydantic 配置类 |
| `backend/packages/harness/deerflow/config/app_config.py` | 修改 | 添加 global_variables 配置加载 |
| `backend/packages/harness/deerflow/config/__init__.py` | 修改 | 导出配置类 |
| `backend/packages/harness/deerflow/global_variables/__init__.py` | 新增 | 模块初始化 |
| `backend/packages/harness/deerflow/global_variables/storage.py` | 新增 | 存储层（含系统变量合并） |
| `backend/packages/harness/deerflow/global_variables/prompt_injector.py` | 新增 | 模板替换 + 提示词注入 |
| `backend/packages/harness/deerflow/global_variables/tools.py` | 新增 | LLM 工具（含权限检查） |
| `backend/packages/harness/deerflow/agents/middlewares/global_variables_middleware.py` | 新增 | 运行时注入中间件 |
| `backend/packages/harness/deerflow/agents/factory.py` | 修改 | 注册中间件到中间件链 |
| `backend/packages/harness/deerflow/agents/lead_agent/prompt.py` | 修改 | get_agent_soul() 调用模板替换 |
| `backend/packages/harness/deerflow/tools/tools.py` | 修改 | 注册全局变量工具到工具组 |
| `backend/app/gateway/routers/global_variables.py` | 新增 | REST API 路由 |
| `backend/app/gateway/routers/__init__.py` | 修改 | 导出路由 |
| `backend/app/gateway/app.py` | 修改 | 注册路由 + OpenAPI 标签 |
| `backend/tests/test_global_variables_storage.py` | 新增 | 存储层单元测试 |
| `backend/tests/test_global_variables_prompt_injection.py` | 新增 | 模板替换单元测试 |
| `backend/tests/test_create_deerflow_agent.py` | 修改 | 更新中间件链预期 |

### 3.2 前端

| 文件 | 操作 | 说明 |
|------|------|------|
| `frontend/src/core/global-variables/api.ts` | 新增 | API 调用函数 |
| `frontend/src/core/global-variables/types.ts` | 新增 | TypeScript 类型定义 |
| `frontend/src/core/global-variables/hooks.ts` | 新增 | React Hooks |
| `frontend/src/components/workspace/settings/global-variables-settings-page.tsx` | 新增 | 管理页面组件 |
| `frontend/src/components/workspace/settings/settings-dialog.tsx` | 修改 | 添加导航项 |
| `frontend/src/components/workspace/settings/index.ts` | 修改 | 导出组件 |
| `frontend/src/core/i18n/locales/types.ts` | 修改 | 类型定义 |
| `frontend/src/core/i18n/locales/zh-CN.ts` | 修改 | 中文翻译 |
| `frontend/src/core/i18n/locales/en-US.ts` | 修改 | 英文翻译 |

---

## 四、使用方法

### 4.1 前端配置

1. 打开 **设置对话框**（右上角齿轮图标）
2. 选择 **全局变量** 菜单项
3. 默认显示项目级变量，可通过下拉切换到对话级
4. 点击 **+ 添加变量** 按钮创建新变量
5. 系统变量（如 `workdir`）以灰色锁定图标显示，不可编辑

### 4.2 在提示词中引用变量

在 SOUL.md 或 SKILL.md 中使用 `{{变量名}}` 语法：

```markdown
## 角色
你是{{role}}，专注于{{domain}}领域。

## 工作目录
当前工作目录：{{workdir}}

## 当前任务
正在处理第{{chapter}}章的内容。
```

### 4.3 大模型修改变量

大模型可通过工具修改当前对话的变量：

```
set_variable(key="chapter", value="6", scope="thread", description="当前章节")
```

### 4.4 外部脚本调用 API

```bash
# 设置项目级变量
curl -X PUT http://localhost:8000/api/global-variables/project/mode \
  -H "Content-Type: application/json" \
  -d '{"value": "writing", "description": "工作模式", "llm_editable": true}'

# 查看变量
curl http://localhost:8000/api/global-variables/project

# 删除变量
curl -X DELETE http://localhost:8000/api/global-variables/project/mode
```

---

## 五、数据结构

### 存储格式

```json
// {DEER_FLOW_HOME}/global_variables.json
{
  "variables": {
    "mode": {
      "value": "writing",
      "description": "工作模式",
      "is_system": false,
      "llm_editable": true,
      "updated_at": "2025-04-27T10:00:00Z",
      "updated_by": "api"
    }
  },
  "lastUpdated": "2025-04-27T10:00:00Z"
}
```

**注意**：系统变量不写入文件，只在内存中合并。

### API 响应格式

```json
{
  "variables": [
    {
      "key": "workdir",
      "value": "/mnt/shared-data",
      "description": "Shared workspace directory",
      "is_system": true,
      "llm_editable": false,
      "updated_at": "system",
      "updated_by": "system"
    },
    {
      "key": "mode",
      "value": "writing",
      "description": "工作模式",
      "is_system": false,
      "llm_editable": true,
      "updated_at": "2025-04-27T10:00:00Z",
      "updated_by": "api"
    }
  ],
  "lastUpdated": "2025-04-27T10:00:00Z"
}
```

---

## 六、测试情况

### 后端单元测试

```
✅ 69 passed in 1.80s
```

| 测试模块 | 用例数 | 状态 |
|---------|--------|------|
| 存储层 | 13 | ✅ |
| 模板替换 | 6 | ✅ |
| 中间件链顺序 | 1 | ✅ |
| 其他回归测试 | 49 | ✅ |

### 前端类型检查

```
✅ 新增文件 0 个类型错误
```

项目原有的 9 个类型错误与本次修改无关。

---

## 七、注意事项

1. **变量引用语法**：使用 `{{变量名}}`（双大括号），不是 `${变量名}`（美元符号）
2. **变量名**：系统变量名是 `workdir`（小写），不是 `__WORKING_DIR__`
3. **模板替换时机**：SOUL.md 加载时替换（项目级变量）+ 运行时中间件替换（含对话级变量）
4. **系统变量保护**：前端只显示锁定图标，API 返回 403 拒绝修改

### 7.1 变量不存在时的显示行为

| 情况 | 提示词中显示 | 示例 |
|------|-------------|------|
| **变量不存在** | 保持原样 `{{变量名}}` | `当前章节：{{chapter}}` |
| **变量存在，值为空字符串** `""` | 显示空白 | `当前章节：` |
| **变量存在，但没设 value 字段** | 保持原样 `{{变量名}}` | `当前章节：{{chapter}}` |

**关键代码**（`prompt_injector.py`）：

```python
def _replacer(match):
    var_name = match.group(1)
    var_data = merged_vars.get(var_name)
    if var_data is None:
        return match.group(0)  # 变量不存在 → 保持原样 {{变量名}}
    if isinstance(var_data, dict):
        return str(var_data.get("value", match.group(0)))  # 没value字段 → 保持原样
    return str(var_data)
```

**总结**：
- 不会显示 `undefined` 或 `null`
- 变量不存在时保留原始占位符，方便排查问题
- 变量值为空字符串时显示空白

---

## 八、如何拓展

### 8.1 新增系统变量

修改 `backend/packages/harness/deerflow/global_variables/storage.py` 中的 `SYSTEM_VARIABLES` 字典：

```python
SYSTEM_VARIABLES: dict[str, Any] = {
    "workdir": {
        "value": "/mnt/shared-data",
        "description": "Shared workspace directory",
        "is_system": True,
        "llm_editable": False,
        "updated_at": "system",
        "updated_by": "system",
    },
    # 新增系统变量 ↓
    "project_name": {
        "value": "我的项目",
        "description": "当前项目名称",
        "is_system": True,
        "llm_editable": False,
        "updated_at": "system",
        "updated_by": "system",
    },
    "model_version": {
        "value": "v2.0",
        "description": "当前模型版本",
        "is_system": True,
        "llm_editable": True,  # 如果希望大模型也能修改
        "updated_at": "system",
        "updated_by": "system",
    },
}
```

**说明**：
- `is_system: True` → 前端显示锁定图标，不可编辑
- `llm_editable: False` → 大模型工具无法修改
- `updated_at: "system"` → 标识为系统内置

### 8.2 新增可配置系统变量（通过 config.yaml）

如果想让系统变量的值可在 `config.yaml` 中自定义：

**步骤1**：在 `global_variables_config.py` 中新增字段

```python
class GlobalVariablesConfig(BaseModel):
    enabled: bool = Field(default=True)
    injection_enabled: bool = Field(default=True)
    # 新增 ↓
    default_workdir: str = Field(default="/mnt/shared-data")
    project_name: str = Field(default="我的项目")
```

**步骤2**：在 `storage.py` 中读取配置

```python
from deerflow.config import get_global_variables_config

def get_system_variables() -> dict[str, Any]:
    config = get_global_variables_config()
    return {
        "variables": {
            "workdir": {
                "value": config.default_workdir,
                "description": "Shared workspace directory",
                "is_system": True,
                "llm_editable": False,
            },
            "project_name": {
                "value": config.project_name,
                "description": "当前项目名称",
                "is_system": True,
                "llm_editable": False,
            },
        },
        "is_system": True,
    }
```

**步骤3**：在 `config.yaml` 中配置

```yaml
global_variables:
  enabled: true
  default_workdir: "/data/shared"
  project_name: "小说创作平台"
```

### 8.3 新增 LLM 可调用工具

在 `backend/packages/harness/deerflow/global_variables/tools.py` 中新增：

```python
@tool("get_all_variables", parse_docstring=True)
def get_all_variables_tool(
    runtime: ToolRuntime[ContextT, ThreadState],
    scope: str = "project",
) -> str:
    """获取所有变量及其值的 JSON 格式。

    Args:
        scope: 作用域，'project' 或 'thread'
    """
    thread_id = _get_thread_id(runtime)
    data = get_storage().load(scope, thread_id=thread_id)
    return json.dumps(data.get("variables", {}), ensure_ascii=False, indent=2)
```

### 8.4 新增变量验证规则

在 `storage.py` 的 `save()` 方法中添加验证：

```python
def save(self, data: dict[str, Any], scope: str, thread_id: str | None = None) -> bool:
    # ...
    # 新增变量名格式验证
    for key in all_vars:
        if not re.match(r"^[a-zA-Z_][a-zA-Z0-9_]*$", key):
            logger.warning("Invalid variable key: %s", key)
            return False
    # ...
```

### 8.5 新增前端页面功能

**新增批量导入功能**：

```typescript
// frontend/src/core/global-variables/api.ts
export async function importVariables(
  scope: VariableScope,
  threadId: string | undefined,
  variables: Record<string, { value: string; description?: string; llm_editable?: boolean }>,
): Promise<VariablesListResponse> {
  const res = await fetch(`${API_BASE}/${scope}/import`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ variables }),
  });
  if (!res.ok) throw new Error("Import failed");
  return res.json();
}
```

### 8.6 新增其他提示词文件的模板替换

除了 SOUL.md，SKILL.md 等其他文件也可以自动替换变量：

在 `backend/packages/harness/deerflow/agents/lead_agent/prompt.py` 中找到对应加载函数，添加 `replace_template_variables()` 调用：

```python
def get_skills_prompt_section(available_skills: set[str] | None = None) -> str:
    from deerflow.global_variables.prompt_injector import replace_template_variables

    # ... 原有加载逻辑 ...
    content = load_skill_file(skill_path)
    content = replace_template_variables(content)  # 新增这行
    # ...
```

---

## 九、后续扩展

- 更多系统变量（如 `project_name`、`model_version` 等）
- 变量导入导出功能
- 变量历史记录/版本回滚
- 变量使用统计（哪些变量被哪些提示词引用）
