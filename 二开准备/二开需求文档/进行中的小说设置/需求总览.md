# 需求总览：进行中的小说设置

> 目标：为对话系统添加小说标签功能，实现对话按小说分组管理，并为后续的全局变量注入和目录限制提供基础

---

## 1. 需求背景

在多小说创作场景中，需要：
- 为每个对话标记所属的小说项目
- 在对话列表中按小说分组展示
- 让 Agent 知道当前正在编辑哪部小说
- 限制文件浏览器只能操作当前小说目录

### 1.1 前置依赖：共享文件夹路径全局变量

**背景**：
项目 `config.yaml` 中配置了一个共享文件夹：
```yaml
sandbox:
  mounts:
    - host_path: c:\xiangmu\deer-flow\backend\.deer-flow\shared-data
      container_path: /mnt/shared-data
      read_only: false
```

**需求**：
1. 系统启动时读取 `config.yaml` 的 `sandbox.mounts` 配置
2. 自动初始化项目级全局变量 `shared_data_path="/mnt/shared-data"`
3. 将该变量注入到所有 Agent 的提示词中
4. 配置修改后重启服务，变量值自动更新

**用途**：
- 所有 Agent 都需要知道共享文件夹路径才能正确读写文件
- 作为全局变量系统的首批变量，验证注入机制

---

## 2. 分期规划

### 一期：标签系统与分组功能

**核心功能**：
- ✅ 新建对话时选择小说目录（非必填）
- ✅ 目录来源于 `sandbox.mounts` 配置下的 `book`/`books` 目录（容器路径）
- ✅ 目录存储为对话级全局变量（系统变量，is_system=True, llm_editable=False）
- ✅ 左侧对话列表按目录分组展示（每次打开默认折叠）
- ✅ 复用现有的全局变量系统（已重构为 SQLite）
- ✅ **存储完整路径，UI 显示时提取最后一段**

**交付物**：
- [前端需求文档](./一期/前端需求-小说标签与分组功能.md)
- [后端需求文档](./一期/后端需求-小说标签系统.md)

**预估工期**：5 天

---

### 二期：全局变量与目录限制

**核心功能**：
- 🔄 将小说标签作为全局变量注入 Agent 提示词
- 🔄 资源管理器限制只能查看当前小说目录

**交付物**：
- [业务需求文档](./二期/业务需求-全局变量与目录限制.md)

**状态**：需求设计完成，等待其他需求明确后开始技术设计

---

## 3. 技术架构概览

### 3.1 数据流

```
┌─────────────────────────────────────────────────────────────┐
│                        一期功能                              │
│                                                              │
│  用户选择目录 → 存储完整路径到 novel_toc → 对话列表分组展示    │
│               （显示时提取最后一段）                          │
└─────────────────────────────────────────────────────────────┘
                              ↓
                    （读取 novel_toc 完整路径）
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                        二期功能                              │
│                                                              │
│  读取 novel_toc → 注入提示词 → 限制资源管理器目录            │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 目录结构

```
/mnt/shared-data/           ← sandbox.mounts 配置的共享目录
└── novels/
    └── book/               ← 标签枚举值来源
        ├── 小说A/
        ├── 小说B/
        └── 小说C/
```

**注意**：实际存储的是完整容器路径，如 `/mnt/shared-data/novels/book/小说A`

---

## 4. 关键技术点

### 4.1 标签来源
- 扫描 `sandbox.mounts` 配置下的 `book`/`books` 目录
- 收集所有一级子文件夹的**容器路径**
- 去重、排序后作为枚举值
- 最多扫描 3 级深度

### 4.2 数据存储
- 使用对话级全局变量系统（已重构为 SQLite）
- 变量名：`novel_toc`
- 类型：对话级系统变量（`is_system=True`, `llm_editable=False`）
- 存储位置：SQLite 数据库 `{DEER_FLOW_HOME}/global_variables.db`
- 存储值为**完整容器路径**（如 `/mnt/shared-data/novels/book/小说A`）
- UI 显示时提取路径最后一段（如"小说A"）
- 可选值：string 或 undefined（未设置时不创建变量）

### 4.3 前端展示
- 新建对话时显示标签选择器（显示小说名，存储完整路径）
- 对话列表按标签分组（分组名提取自路径最后一段）
- 分组支持展开/折叠（不保留状态）

### 4.4 二期注入（概要）
- 提示词模板变量：`{novel_tag}`
- 资源管理器根目录：`/mnt/user-data/workspace/book/{novel_tag}/`

---

## 5. 文件清单

### 一期

#### 前端
| 文件 | 类型 | 说明 |
|------|------|------|
| `frontend/src/components/workspace/novel-tag-selector.tsx` | 新增 | 标签选择器组件 |
| `frontend/src/core/novel-tags/hooks.ts` | 新增 | 标签 API hooks |
| `frontend/src/core/novel-tags/types.ts` | 新增 | 类型定义 |
| `frontend/src/components/workspace/input-box.tsx` | 修改 | 集成标签选择器 |
| `frontend/src/components/workspace/recent-chat-list.tsx` | 修改 | 添加分组功能 |
| `frontend/src/core/threads/hooks.ts` | 修改 | 在 onCreated 中保存 novel_tag 到全局变量 |

#### 后端
| 文件 | 类型 | 说明 |
|------|------|------|
| `backend/app/gateway/routers/novel_tags.py` | 新增 | 标签 API（带缓存，扫描 sandbox.mounts） |
| `backend/app/gateway/app.py` | 修改 | 注册路由 |
| `backend/app/gateway/routers/__init__.py` | 修改 | 导出模块 |

---

## 6. 依赖关系

```
一期（可独立交付）
├── 目录选择功能
├── 目录存储功能（完整路径）
└── 分组展示功能（显示名提取）
        ↓
    提供 novel_toc 数据（完整路径）
        ↓
二期（依赖一期）
├── 全局变量注入
└── 目录访问限制
```

---

## 7. 风险与注意事项

### 7.1 性能
- 一期标签扫描可能较慢，需要实现缓存机制
- 缓存 TTL 为 5 分钟
- 当前数据量不大，无需复杂优化

### 7.2 兼容性
- 已有对话没有 novel_tag，前端需兼容显示为"未分类"
- 后端 API 需处理 novel_tag 为空的情况

### 7.3 安全性
- 二期目录限制必须在后端强制验证
- 不能仅依赖前端 UI 限制

### 7.4 业务规则
- **标签非必填**：用户可以不选标签
- **标签不可修改**：创建后锁定，只有新建对话时可以选择
- **不保留分组状态**：每次打开页面，所有分组默认折叠
- **后端不验证标签**：允许任意标签值
- **标签为系统变量**：`is_system=True`，前端只读展示
- **大模型不可修改**：`llm_editable=False`

---

## 8. 后续工作

### 待明确的需求
1. 全局变量的具体实现方案（需求文档待完善）
2. 资源管理器组件的具体实现（目录管理需求进行中）
3. 提示词注入的具体格式和位置

### 可能的优化
1. 标签支持多级分类
2. 标签支持搜索和筛选
3. 对话支持修改标签（一期锁定不可修改）
4. 标签云或标签统计功能

---

## 9. 术语表

| 术语 | 说明 |
|------|------|
| novel_toc | 小说目录（Table of Contents），存储的是完整容器路径，UI 显示时提取最后一段 |
| book 目录 | sandbox.mounts 配置下的共享目录下的 book/books 文件夹，存放各小说项目 |
| 对话级全局变量 | 存储在 SQLite 数据库中的变量，每个对话独立 |
| 系统变量 | `is_system=True` 的变量，前端只读，大模型不可修改 |
| 分组 | 对话列表中按 novel_toc 提取的名称归类的展示方式 |
| 全局变量系统 | 已开发完成的变量管理系统，支持 SQLite 存储两级隔离和模板替换 |
